$DASH_DOMAIN {
	encode gzip
	# API → container Node
	handle /api/* {
		reverse_proxy app:8787
	}
	# resto → front estático (SPA fallback)
	handle {
		root * /srv
		try_files {path} /index.html
		file_server
	}
}
